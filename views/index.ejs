<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="icon" type="image/x-icon"
        href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🍸</text></svg>">
</head>

<body>
    <div class="container">
        <header>
            <h1>🍸 Sipify</h1>
            <p class="subtitle">Discover amazing cocktails from around the world</p>
        </header>

        <main>
            <div class="main-content fade-in">
                <div class="cocktail-card">
                    <div class="cocktail-image">
                        <img src="<%= cocktail.strDrinkThumb %>" alt="<%= cocktail.strDrink %>" loading="lazy">
                    </div>

                    <div class="cocktail-info">
                        <div>
                            <h2 class="cocktail-name">
                                <%= cocktail.strDrink %>
                            </h2>

                            <span class="cocktail-category">
                                <%= cocktail.strCategory %>
                            </span>

                            <p class="cocktail-glass">
                                <strong>Served in:</strong>
                                <%= cocktail.strGlass %>
                            </p>

                            <div class="ingredients-section">
                                <h3 class="section-title">Ingredients</h3>
                                <ul class="ingredients-list">
                                    <% ingredients.forEach(function(item) { %>
                                        <li class="ingredient-item">
                                            <% if (item.measurement) { %>
                                                <span class="measurement">
                                                    <%= item.measurement %>
                                                </span>
                                                <% } %>
                                                    <span class="ingredient">
                                                        <%= item.ingredient %>
                                                    </span>
                                        </li>
                                        <% }); %>
                                </ul>
                            </div>

                            <div class="instructions-section">
                                <h3 class="section-title">Instructions</h3>
                                <div class="instructions">
                                    <%= cocktail.strInstructions %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="action-section">
                <form method="POST" action="/new-cocktail" style="display: inline; ">
                    <button type="submit" class="btn-new-cocktail">
                        🎲 Get Another Cocktail
                    </button>
                </form>
            </div>
        </main>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const forms = document.querySelectorAll('form[action="/new-cocktail"]');
            forms.forEach(form => {
                form.addEventListener('submit', function () {
                    const button = this.querySelector('.btn-new-cocktail');
                    button.innerHTML = 'Getting New Cocktail<span class="loading"></span>';
                    button.disabled = true; // optional, prevents double click
                });
            });
        });
    </script>

</body>

</html>